name: Deploy to GitHub Pages

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: 'pages'
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'pnpm'

      - name: Install deps
        run: pnpm install --frozen-lockfile

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Detect base path
        id: detect
        run: |
          REPO_NAME="${GITHUB_REPOSITORY##*/}"
          # If repository is <user>.github.io, base path should be empty
          if [[ "$REPO_NAME" =~ \.github\.io$ ]]; then
            echo "base_path=" >> $GITHUB_OUTPUT
            echo "base_url=https://${GITHUB_REPOSITORY}" >> $GITHUB_OUTPUT
          else
            echo "base_path=/${REPO_NAME}" >> $GITHUB_OUTPUT
            echo "base_url=https://${GITHUB_REPOSITORY%/*}.github.io/${REPO_NAME}" >> $GITHUB_OUTPUT
          fi

      - name: Build and Export
        env:
          # Next.js basePath/assetPrefix
          BASE_PATH: ${{ steps.detect.outputs.base_path }}
          # For absolute URLs in RSS
          BASE_URL: ${{ steps.detect.outputs.base_url }}
        run: |
          pnpm build
          pnpm preexport
          pnpm export

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./out

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
